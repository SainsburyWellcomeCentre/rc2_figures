function val = constants(variable_name)

const.size_20cmps = 2.735;  % size of scale bar in mm on page (determines axis limits)
const.size_30cmps = 4.998;  % size of scale bar in mm on page (determines axis limits)

val = const.(variable_name);